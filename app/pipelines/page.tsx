import Pipelines from "@/src/components/platform/Pipelines";

export const metadata = {
  title: "Pipelines — Formatter",
  description: "Chain developer tools so each step's output feeds the next — entirely in your browser.",
};

export default function PipelinesPage() {
  return <Pipelines />;
}
