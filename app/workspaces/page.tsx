import Workspaces from "@/src/components/platform/Workspaces";

export const metadata = {
  title: "Workspaces — Formatter",
  description: "Save tool inputs and options in named workspaces, stored only in your browser.",
};

export default function WorkspacesPage() {
  return <Workspaces />;
}
