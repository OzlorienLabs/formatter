import Recipes from "@/src/components/platform/Recipes";

export const metadata = {
  title: "Recipes — Formatter",
  description: "Ready-made, explained multi-step workflows you can run in one click.",
};

export default function RecipesPage() {
  return <Recipes />;
}
